# Dependencies (reproducible from package.json / package-lock.json)
node_modules/

# Local secrets and config
creds.env
*.env

# DuckDB database + write-ahead logs
*.db
*.db.wal
*.wal

# Runtime output / logs
logs/
*.log
api_out.json

# Chromium profile / session data
testChromeSession/

# OS cruft
Thumbs.db
Desktop.ini
.DS_Store

# DuckLake catalog + data
*.ducklake
*.ducklake.files/

# Data content (repo keeps only utility scripts under data/scripts/)
data/*
!data/scripts/

# Local-only working folders (AI transcripts, ignored/backup code)
ai/
ign/
